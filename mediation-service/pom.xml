<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.telecom</groupId>
        <artifactId>telecom-parent</artifactId>
        <version>1.0</version>
    </parent>

    <artifactId>mediation-service</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>org.telecom</groupId>
            <artifactId>telecom-common</artifactId>
            <version>1.0</version>
        </dependency>
        <dependency>
            <groupId>org.telecom</groupId>
            <artifactId>msc-service</artifactId>
            <version>1.0</version>
        </dependency>
        <dependency>
            <groupId>org.telecom</groupId>
            <artifactId>pgw-service</artifactId>
            <version>1.0</version>
        </dependency>
        <dependency>
            <groupId>org.telecom</groupId>
            <artifactId>smsc-service</artifactId>
            <version>1.0</version>
        </dependency>
        <!-- HikariCP connection pool — keeps a warm pool of DB connections
             so each CDR insert doesn't pay the TCP + TLS handshake cost -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>7.0.2</version>
            <scope>compile</scope>
        </dependency>
        <!-- PostgreSQL JDBC driver — must be on the classpath at runtime so
             HikariCP can load org.postgresql.Driver via Class.forName().
             Without this the fat JAR starts but every DB call throws
             ExceptionInInitializerError: Failed to load driver class. -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.3</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk15on</artifactId>
            <version>1.70</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.0</version>

                <executions>
                    <execution>
                        <phase>package</phase>

                        <goals>
                            <goal>shade</goal>
                        </goals>

                        <configuration>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>mediation.Main</mainClass>
                                </transformer>
                            </transformers>

                        </configuration>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>
    <name>Mediation Service</name>
    <description>Telecom Mediation Service</description>
</project>